entity: Provider
items:
  provider{1..100}:
    name: "{{company.companyName}}"
    code: "{{company.companySuffix}}"
    address: "{{address.direction}}"
    description: "{{company.catchPhraseDescriptor}}"
    enabled: true
